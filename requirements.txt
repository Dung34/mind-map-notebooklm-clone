# HTTP + API
httpx>=0.27
fastapi>=0.115
uvicorn>=0.30

# Config + validation
pydantic>=2.7
pydantic-settings>=2.3
python-dotenv>=1.0

# External services
perplexityai>=0.1.0
firecrawl-py>=1.0.0
tenacity>=8.3

# Database + migrations
sqlalchemy>=2.0
alembic>=1.13
psycopg[binary]>=3.2

# Phase 10 MindMap (UMAP + HDBSCAN + NER + OPML; scipy pulled in by umap-learn)
numpy>=1.26
umap-learn>=0.5
hdbscan>=0.8
scikit-learn>=1.4
spacy>=3.7
lxml>=5.2
